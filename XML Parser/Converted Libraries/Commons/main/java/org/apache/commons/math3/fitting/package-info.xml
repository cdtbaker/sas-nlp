<?xml version="1.0" encoding="UTF-8"?>
<source package="Classes to perform curve fitting.
 * Curve fitting is a special case of a least squares problem
 * were the parameters are the coefficients of a function {@code f}whose graph {@code y = f(x)} should pass through sample points, and
 * were the objective function is the squared sum of the residuals
 * <code>f(x<sub>i</sub>) - y<sub>i</sub></code> for observed points
 * <code>(x<sub>i</sub>, y<sub>i</sub>)</code>.
 */
package org.apache.commons.math3.fitting">
  <javadoc>
    <text>* Classes to perform curve fitting.
 * Curve fitting is a special case of a least squares problem
 * were the parameters are the coefficients of a function {@code f}whose graph {@code y = f(x)} should pass through sample points, and
 * were the objective function is the squared sum of the residuals
 * <code>f(x<sub>i</sub>) - y<sub>i</sub></code> for observed points
 * <code>(x<sub>i</sub>, y<sub>i</sub>)</code>.</text>
  </javadoc>
</source>
