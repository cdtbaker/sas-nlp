<?xml version="1.0" encoding="UTF-8"?>
    <source package="com.sun.rowset">
        <import package="java.io"/>
        <import package="java.util"/>
        <import package="java.sql"/>
        <import package="javax.sql"/>
        <import package="java.math"/>
        <import package="javax.sql.rowset"/>
        <import package="javax.sql.rowset.spi"/>
        <import package="javax.sql.rowset.serial"/>
        <import package="com.sun.rowset.providers"/>
        <import package="com.sun.rowset.internal"/>
        <class name="FilteredRowSetImpl" line="40">
            <extends class="WebRowSetImpl"/>
            <comment line="240">
                end internalLast()                
            </comment>
            <comment line="451">
                This is explanation for the overriding of the updateXXX functions.
                 These functions have been overriden to ensure that only correct
                 values that pass the criteria for the filter are actaully inserted.
                 The evaluation of whether a particular value passes the criteria
                 of the filter is done using the evaluate function in the Predicate
                 interface.
                
                 The checking can will done in the evaluate function which is implemented
                 in the class that implements the Predicate interface. So the checking
                 can vary from one implementation to another.
                
                 Some additional points here on the following:
                 1. updateBytes()     - since the evaluate function takes Object as parameter
                                        a String is constructed from the byte array and would
                                        passed to the evaluate function.
                 2. updateXXXstream() - here it would suffice to pass the stream handle
                                        to the evaluate function and the implementation
                                        of the evaluate function can do the comparision
                                        based on the stream and also type of data.                
            </comment>
            <implements interface="Serializable"/>
            <implements interface="Cloneable"/>
            <implements interface="FilteredRowSet"/>
            <javadoc line="40">
                The standard implementation of the &lt;code&gt;FilteredRowSet&lt;/code&gt; interface. See the interface
                  defintion for full behaviour and implementation requirements.                
                <see>
                    javax.sql.rowset.Predicate                    
                </see>
                <author>
                    Jonathan Bruce, Amit Handa                    
                </author>
            </javadoc>
            <declaration name="p" type="Predicate" line="50"/>
            <declaration name="onInsertRow" type="boolean" line="52"/>
            <javadoc line="55">
                Construct a &lt;code&gt;FilteredRowSet&lt;/code&gt;                
            </javadoc>
            <method name="FilteredRowSetImpl" type="constructor" line="58"/>
            <javadoc line="62">
                Construct a &lt;code&gt;FilteredRowSet&lt;/code&gt; with a specified synchronization
                  provider.                
                <param>
                    env a Hashtable containing a desired synchconizatation provider
                      name-value pair.                    
                </param>
            </javadoc>
            <method name="FilteredRowSetImpl" type="constructor" line="69">
                <params>
                    <param name="env" type="Hashtable"/>
                </params>
            </method>
            <javadoc line="73">
                Apply the predicate for this filter                
                <param>
                    p an implementation of the predicate interface                    
                </param>
            </javadoc>
            <method name="setFilter" type="void" line="78">
                <params>
                    <param name="p" type="Predicate"/>
                </params>
            </method>
            <javadoc line="82">
                Retrieve the filter active for this &lt;code&gt;FilteredRowSet&lt;/code&gt;                
                <return>
                    a &lt;code&gt;Predicate&lt;/code&gt; object instance                    
                </return>
            </javadoc>
            <method name="getFilter" type="Predicate" line="87"/>
            <javadoc line="91">
                Over-riding &lt;code&gt;internalNext()&lt;/code&gt; implementation. This method
                  applies the filter on the &lt;code&gt;RowSet&lt;/code&gt; each time the cursor is advanced or
                  manipulated. It moves the cursor to the next row according to the set
                  predicate and returns &lt;code&gt;true&lt;/code&gt; if the cursor is still within the rowset or
                  &lt;code&gt;false&lt;/code&gt; if the cursor position is over the last row                
                <return>
                    true if over the valid row in the rowset; false if over the last
                      row                    
                </return>
            </javadoc>
            <method name="internalNext" type="boolean" line="101">
                <comment line="103">
                    CachedRowSetImpl.next() internally calls
                     this(crs).internalNext() NOTE: this holds crs object
                     So when frs.next() is called,
                     internally this(frs).internalNext() will be called
                     which will be nothing but this method.
                     because this holds frs object                    
                </comment>
                <comment line="110">
                    keep on doing super.internalNext()
                     rather than doing it once.                    
                </comment>
                <comment line="114">
                    p.evaluate will help us in changing the cursor
                     and checking the next value by returning true or false.
                     to fit the filter                    
                </comment>
                <comment line="118">
                    So while() loop will have a &quot;random combination&quot; of
                     true and false returned depending upon the records
                     are in or out of filter.
                     We need to traverse from present cursorPos till end,
                     whether true or false and check each row for &quot;filter&quot;
                     &quot;till we get a &quot;true&quot;                    
                </comment>
                <declaration name="bool" type="boolean" line="125"/>
                <scope line="127">
                    <scope line="130"/>
                    <scope line="133"/>
                </scope>
            </method>
            <javadoc line="143">
                Over-riding &lt;code&gt;internalPrevious()&lt;/code&gt; implementation. This method
                  applies the filter on the &lt;code&gt;RowSet&lt;/code&gt; each time the cursor is moved backward or
                  manipulated. It moves the cursor to the previous row according to the set
                  predicate and returns &lt;code&gt;true&lt;/code&gt; if the cursor is still within the rowset or
                  &lt;code&gt;false&lt;/code&gt; if the cursor position is over the last row                
                <return>
                    true if over the valid row in the rowset; false if over the last
                      row                    
                </return>
            </javadoc>
            <method name="internalPrevious" type="boolean" line="153">
                <comment line="156">
                    with previous move backwards,
                     i.e. from any record towards first record                    
                </comment>
                <declaration name="bool" type="boolean" line="154"/>
                <scope line="158">
                    <scope line="162"/>
                    <scope line="166"/>
                </scope>
            </method>
            <javadoc line="176">
                Over-riding &lt;code&gt;internalFirst()&lt;/code&gt; implementation. This method
                  applies the filter on the &lt;code&gt;RowSet&lt;/code&gt; each time the cursor is moved to first
                  row. It moves the cursor to the first row according to the set
                  predicate and returns &lt;code&gt;true&lt;/code&gt; if the cursor is still within the rowset or
                  &lt;code&gt;false&lt;/code&gt; if the cursor position is over the last row                
                <return>
                    true if over the valid row in the rowset; false if over the last
                      row                    
                </return>
            </javadoc>
            <method name="internalFirst" type="boolean" line="186">
                <comment line="189">
                    from first till present cursor position(go forward),
                     find the actual first which matches the filter.                    
                </comment>
                <declaration name="bool" type="boolean" line="191"/>
                <scope line="193"/>
                <scope line="197">
                    <scope line="199"/>
                </scope>
            </method>
            <javadoc line="208">
                Over-riding &lt;code&gt;internalLast()&lt;/code&gt; implementation. This method
                  applies the filter on the &lt;code&gt;RowSet&lt;/code&gt; each time the cursor is moved to
                  last row. It moves the cursor to the last row according to the set
                  predicate and returns &lt;code&gt;true&lt;/code&gt; if the cursor is still within the rowset or
                  &lt;code&gt;false&lt;/code&gt; if the cursor position is over the last row                
                <return>
                    true if over the valid row in the rowset; false if over the last
                      row                    
                </return>
            </javadoc>
            <method name="internalLast" type="boolean" line="218">
                <comment line="220">
                    from last to the present cursor position(go backward),
                     find the actual last which matches the filter.                    
                </comment>
                <declaration name="bool" type="boolean" line="222"/>
                <scope line="224"/>
                <scope line="228">
                    <scope line="230"/>
                </scope>
            </method>
            <javadoc line="240">
                Moves the cursor the specified number of rows from the current
                  position, with a positive number moving it forward and a
                  negative number moving it backward.
                  &lt;P&gt;
                  If the number is positive, the cursor moves the specified number of
                  rows toward the end of the rowset, starting at the current row.
                  For example, the following command, in which
                  &lt;code&gt;crs&lt;/code&gt; is a &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with 100 rows,
                  moves the cursor forward four rows from the current row.  If the
                  current row is 50, the cursor would move to row 54.
                  &lt;PRE&gt;&lt;code&gt;
                  crs.relative(4);
                  &lt;/code&gt; &lt;/PRE&gt;
                  &lt;P&gt;
                  If the number is negative, the cursor moves back toward the beginning
                  the specified number of rows, starting at the current row.
                  For example, calling the method
                  &lt;code&gt;absolute(-1)&lt;/code&gt; positions the cursor on the last row,
                  &lt;code&gt;absolute(-2)&lt;/code&gt; moves it on the next-to-last row, and so on.
                  If the &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object &lt;code&gt;crs&lt;/code&gt; has five rows,
                  the following command moves the cursor to the fourth-to-last row, which
                  in the case of a  rowset with five rows, is also the second row
                  from the beginning.
                  &lt;PRE&gt;&lt;code&gt;
                  crs.absolute(-4);
                  &lt;/code&gt; &lt;/PRE&gt;
                  If the number specified is larger than the number of rows, the cursor
                  will move to the position after the last row. If the number specified
                  would move the cursor one or more rows before the first row, the cursor
                  moves to the position before the first row. In both cases, this method
                  throws an &lt;code&gt;SQLException&lt;/code&gt;.
                  &lt;P&gt;
                  Note: Calling &lt;code&gt;absolute(1)&lt;/code&gt; is the same as calling the
                  method &lt;code&gt;first()&lt;/code&gt;.  Calling &lt;code&gt;absolute(-1)&lt;/code&gt; is the
                  same as calling &lt;code&gt;last()&lt;/code&gt;.  Calling &lt;code&gt;relative(0)&lt;/code&gt;
                  is valid, but it does not change the cursor position.                
                <param>
                    rows an &lt;code&gt;int&lt;/code&gt; indicating the number of rows to move
                      the cursor, starting at the current row; a positive number
                      moves the cursor forward; a negative number moves the cursor
                      backward; must not move the cursor past the valid
                      rows                    
                </param>
                <return>
                    &lt;code&gt;true&lt;/code&gt; if the cursor is on a row in this
                      &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object; &lt;code&gt;false&lt;/code&gt;
                      otherwise                    
                </return>
                <throws>
                    SQLException if the rowset is type &lt;code&gt;ResultSet.TYPE_FORWARD_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="relative" type="boolean" line="293">
                <params>
                    <param name="rows" type="int"/>
                </params>
                <declaration name="retval" type="boolean" line="295"/>
                <declaration name="bool" type="boolean" line="296"/>
                <declaration name="boolval" type="boolean" line="297"/>
                <scope line="299"/>
                <scope line="303">
                    <declaration name="i" type="int" line="305"/>
                    <scope line="306">
                        <scope line="308"/>
                    </scope>
                </scope>
                <scope line="316">
                    <declaration name="j" type="int" line="317"/>
                    <scope line="318">
                        <scope line="320"/>
                    </scope>
                </scope>
            </method>
            <javadoc line="333">
                Moves this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object&apos;s cursor to the row number
                  specified.
                  &lt;p&gt;If the number is positive, the cursor moves to an absolute row with
                  respect to the beginning of the rowset.  The first row is row 1, the second
                  is row 2, and so on.  For example, the following command, in which
                  &lt;code&gt;crs&lt;/code&gt; is a &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object, moves the cursor
                  to the fourth row, starting from the beginning of the rowset.
                  &lt;PRE&gt;&lt;code&gt;
                  crs.absolute(4);
                  &lt;/code&gt; &lt;/PRE&gt;
                  &lt;P&gt;
                  If the number is negative, the cursor moves to an absolute row position
                  with respect to the end of the rowset.  For example, calling
                  &lt;code&gt;absolute(-1)&lt;/code&gt; positions the cursor on the last row,
                  &lt;code&gt;absolute(-2)&lt;/code&gt; moves it on the next-to-last row, and so on.
                  If the &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object &lt;code&gt;crs&lt;/code&gt; has five rows,
                  the following command moves the cursor to the fourth-to-last row, which
                  in the case of a  rowset with five rows, is also the second row, counting
                  from the beginning.
                  &lt;PRE&gt;&lt;code&gt;
                  crs.absolute(-4);
                  &lt;/code&gt; &lt;/PRE&gt;
                  If the number specified is larger than the number of rows, the cursor
                  will move to the position after the last row. If the number specified
                  would move the cursor one or more rows before the first row, the cursor
                  moves to the position before the first row.
                  &lt;P&gt;
                  Note: Calling &lt;code&gt;absolute(1)&lt;/code&gt; is the same as calling the
                  method &lt;code&gt;first()&lt;/code&gt;.  Calling &lt;code&gt;absolute(-1)&lt;/code&gt; is the
                  same as calling &lt;code&gt;last()&lt;/code&gt;.                
                <param>
                    rows a positive number to indicate the row, starting row numbering from
                      the first row, which is &lt;code&gt;1&lt;/code&gt;; a negative number to indicate
                      the row, starting row numbering from the last row, which is
                      &lt;code&gt;-1&lt;/code&gt;; it must not be &lt;code&gt;0&lt;/code&gt;                    
                </param>
                <return>
                    &lt;code&gt;true&lt;/code&gt; if the cursor is on the rowset; &lt;code&gt;false&lt;/code&gt;
                      otherwise                    
                </return>
                <throws>
                    SQLException if the given cursor position is &lt;code&gt;0&lt;/code&gt; or the
                      type of this rowset is &lt;code&gt;ResultSet.TYPE_FORWARD_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="absolute" type="boolean" line="380">
                <params>
                    <param name="rows" type="int"/>
                </params>
                <declaration name="retval" type="boolean" line="382"/>
                <declaration name="bool" type="boolean" line="383"/>
                <scope line="385"/>
                <scope line="389">
                    <declaration name="i" type="int" line="392"/>
                    <scope line="393">
                        <scope line="394"/>
                    </scope>
                </scope>
                <scope line="401">
                    <declaration name="j" type="int" line="404"/>
                    <scope line="405">
                        <scope line="406"/>
                    </scope>
                </scope>
            </method>
            <javadoc line="418">
                Moves the cursor for this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object
                  to the insert row.  The current row in the rowset is remembered
                  while the cursor is on the insert row.
                  &lt;P&gt;
                  The insert row is a special row associated with an updatable
                  rowset.  It is essentially a buffer where a new row may
                  be constructed by calling the appropriate &lt;code&gt;updateXXX&lt;/code&gt;
                  methods to assign a value to each column in the row.  A complete
                  row must be constructed; that is, every column that is not nullable
                  must be assigned a value.  In order for the new row to become part
                  of this rowset, the method &lt;code&gt;insertRow&lt;/code&gt; must be called
                  before the cursor is moved back to the rowset.
                  &lt;P&gt;
                  Only certain methods may be invoked while the cursor is on the insert
                  row; many methods throw an exception if they are called while the
                  cursor is there.  In addition to the &lt;code&gt;updateXXX&lt;/code&gt;
                  and &lt;code&gt;insertRow&lt;/code&gt; methods, only the &lt;code&gt;getXXX&lt;/code&gt; methods
                  may be called when the cursor is on the insert row.  A &lt;code&gt;getXXX&lt;/code&gt;
                  method should be called on a column only after an &lt;code&gt;updateXXX&lt;/code&gt;
                  method has been called on that column; otherwise, the value returned is
                  undetermined.                
                <throws>
                    SQLException if this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object is
                      &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="moveToInsertRow" type="void" line="444"/>
            <javadoc line="473">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;int&lt;/code&gt; value.
                  &lt;P&gt;
                  This method updates a column value in the current row or the insert
                  row of this rowset, but it does not update the database.
                  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnIndex the first column is &lt;code&gt;1&lt;/code&gt;, the second
                      is &lt;code&gt;2&lt;/code&gt;, and so on; must be &lt;code&gt;1&lt;/code&gt; or larger
                      and equal to or less than the number of columns in this rowset                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <throws>
                    SQLException if (1) the given column index is out of bounds,
                      (2) the cursor is not on one of this rowset&apos;s rows or its
                      insert row, or (3) this rowset is
                      &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateInt" type="void" line="496">
                <params>
                    <param name="columnIndex" type="int"/>
                    <param name="x" type="int"/>
                </params>
                <declaration name="bool" type="boolean" line="498"/>
                <scope line="500">
                    <scope line="501">
                        <scope line="504"/>
                    </scope>
                </scope>
            </method>
            <javadoc line="513">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;int&lt;/code&gt; value.
                  &lt;P&gt;
                  This method updates a column value in the current row or the insert
                  row of this rowset, but it does not update the database.
                  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnName a &lt;code&gt;String&lt;/code&gt; object that must match the
                      SQL name of a column in this rowset, ignoring case                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <throws>
                    SQLException if (1) the given column name does not match the
                      name of a column in this rowset, (2) the cursor is not on
                      one of this rowset&apos;s rows or its insert row, or (3) this
                      rowset is &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateInt" type="void" line="535">
                <params>
                    <param name="columnName" type="String"/>
                    <param name="x" type="int"/>
                </params>
            </method>
            <javadoc line="540">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;boolean&lt;/code&gt; value.
                  &lt;P&gt;
                  This method updates a column value in the current row or the insert
                  row of this rowset, but it does not update the database.
                  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnIndex the first column is &lt;code&gt;1&lt;/code&gt;, the second
                      is &lt;code&gt;2&lt;/code&gt;, and so on; must be &lt;code&gt;1&lt;/code&gt; or larger
                      and equal to or less than the number of columns in this rowset                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <throws>
                    SQLException if (1) the given column index is out of bounds,
                      (2) the cursor is not on one of this rowset&apos;s rows or its
                      insert row, or (3) this rowset is
                      &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateBoolean" type="void" line="563">
                <params>
                    <param name="columnIndex" type="int"/>
                    <param name="x" type="boolean"/>
                </params>
                <declaration name="bool" type="boolean" line="565"/>
                <scope line="567">
                    <scope line="568">
                        <scope line="571"/>
                    </scope>
                </scope>
            </method>
            <javadoc line="580">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;boolean&lt;/code&gt; value.
                  &lt;P&gt;
                  This method updates a column value in the current row or the insert
                  row of this rowset, but it does not update the database.
                  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnName a &lt;code&gt;String&lt;/code&gt; object that must match the
                      SQL name of a column in this rowset, ignoring case                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <throws>
                    SQLException if (1) the given column name does not match the
                      name of a column in this rowset, (2) the cursor is not on
                      one of this rowset&apos;s rows or its insert row, or (3) this
                      rowset is &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateBoolean" type="void" line="602">
                <params>
                    <param name="columnName" type="String"/>
                    <param name="x" type="boolean"/>
                </params>
            </method>
            <javadoc line="609">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;byte&lt;/code&gt; value.
                  &lt;P&gt;
                  This method updates a column value in the current row or the insert
                  row of this rowset, but it does not update the database.
                  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnIndex the first column is &lt;code&gt;1&lt;/code&gt;, the second
                      is &lt;code&gt;2&lt;/code&gt;, and so on; must be &lt;code&gt;1&lt;/code&gt; or larger
                      and equal to or less than the number of columns in this rowset                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <throws>
                    SQLException if (1) the given column index is out of bounds,
                      (2) the cursor is not on one of this rowset&apos;s rows or its
                      insert row, or (3) this rowset is
                      &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateByte" type="void" line="632">
                <params>
                    <param name="columnIndex" type="int"/>
                    <param name="x" type="byte"/>
                </params>
                <declaration name="bool" type="boolean" line="633"/>
                <scope line="635">
                    <scope line="636">
                        <scope line="639"/>
                    </scope>
                </scope>
            </method>
            <javadoc line="649">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;byte&lt;/code&gt; value.
                  &lt;P&gt;
                  This method updates a column value in the current row or the insert
                  row of this rowset, but it does not update the database.
                  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnName a &lt;code&gt;String&lt;/code&gt; object that must match the
                      SQL name of a column in this rowset, ignoring case                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <throws>
                    SQLException if (1) the given column name does not match the
                      name of a column in this rowset, (2) the cursor is not on
                      one of this rowset&apos;s rows or its insert row, or (3) this
                      rowset is &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateByte" type="void" line="671">
                <params>
                    <param name="columnName" type="String"/>
                    <param name="x" type="byte"/>
                </params>
            </method>
            <javadoc line="677">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;short&lt;/code&gt; value.
                  &lt;P&gt;
                  This method updates a column value in the current row or the insert
                  row of this rowset, but it does not update the database.
                  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnIndex the first column is &lt;code&gt;1&lt;/code&gt;, the second
                      is &lt;code&gt;2&lt;/code&gt;, and so on; must be &lt;code&gt;1&lt;/code&gt; or larger
                      and equal to or less than the number of columns in this rowset                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <throws>
                    SQLException if (1) the given column index is out of bounds,
                      (2) the cursor is not on one of this rowset&apos;s rows or its
                      insert row, or (3) this rowset is
                      &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateShort" type="void" line="700">
                <params>
                    <param name="columnIndex" type="int"/>
                    <param name="x" type="short"/>
                </params>
                <declaration name="bool" type="boolean" line="702"/>
                <scope line="704">
                    <scope line="705">
                        <scope line="708"/>
                    </scope>
                </scope>
            </method>
            <javadoc line="717">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;short&lt;/code&gt; value.
                  &lt;P&gt;
                  This method updates a column value in the current row or the insert
                  row of this rowset, but it does not update the database.
                  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnName a &lt;code&gt;String&lt;/code&gt; object that must match the
                      SQL name of a column in this rowset, ignoring case                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <throws>
                    SQLException if (1) the given column name does not match the
                      name of a column in this rowset, (2) the cursor is not on
                      one of this rowset&apos;s rows or its insert row, or (3) this
                      rowset is &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateShort" type="void" line="739">
                <params>
                    <param name="columnName" type="String"/>
                    <param name="x" type="short"/>
                </params>
            </method>
            <javadoc line="745">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;long&lt;/code&gt; value.
                  &lt;P&gt;
                  This method updates a column value in the current row or the insert
                  row of this rowset, but it does not update the database.
                  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnIndex the first column is &lt;code&gt;1&lt;/code&gt;, the second
                      is &lt;code&gt;2&lt;/code&gt;, and so on; must be &lt;code&gt;1&lt;/code&gt; or larger
                      and equal to or less than the number of columns in this rowset                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <throws>
                    SQLException if (1) the given column index is out of bounds,
                      (2) the cursor is not on one of this rowset&apos;s rows or its
                      insert row, or (3) this rowset is
                      &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateLong" type="void" line="768">
                <params>
                    <param name="columnIndex" type="int"/>
                    <param name="x" type="long"/>
                </params>
                <declaration name="bool" type="boolean" line="770"/>
                <scope line="772">
                    <scope line="773">
                        <scope line="776"/>
                    </scope>
                </scope>
            </method>
            <javadoc line="785">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;long&lt;/code&gt; value.
                  &lt;P&gt;
                  This method updates a column value in the current row or the insert
                  row of this rowset, but it does not update the database.
                  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnName a &lt;code&gt;String&lt;/code&gt; object that must match the
                      SQL name of a column in this rowset, ignoring case                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <throws>
                    SQLException if (1) the given column name does not match the
                      name of a column in this rowset, (2) the cursor is not on
                      one of this rowset&apos;s rows or its insert row, or (3) this
                      rowset is &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateLong" type="void" line="807">
                <params>
                    <param name="columnName" type="String"/>
                    <param name="x" type="long"/>
                </params>
            </method>
            <javadoc line="812">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;float&lt;/code&gt; value.
                  &lt;P&gt;
                  This method updates a column value in the current row or the insert
                  row of this rowset, but it does not update the database.
                  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnIndex the first column is &lt;code&gt;1&lt;/code&gt;, the second
                      is &lt;code&gt;2&lt;/code&gt;, and so on; must be &lt;code&gt;1&lt;/code&gt; or larger
                      and equal to or less than the number of columns in this rowset                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <throws>
                    SQLException if (1) the given column index is out of bounds,
                      (2) the cursor is not on one of this rowset&apos;s rows or its
                      insert row, or (3) this rowset is
                      &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateFloat" type="void" line="835">
                <params>
                    <param name="columnIndex" type="int"/>
                    <param name="x" type="float"/>
                </params>
                <declaration name="bool" type="boolean" line="837"/>
                <scope line="839">
                    <scope line="840">
                        <scope line="843"/>
                    </scope>
                </scope>
            </method>
            <javadoc line="852">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;float&lt;/code&gt; value.
                  &lt;P&gt;
                  This method updates a column value in the current row or the insert
                  row of this rowset, but it does not update the database.
                  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnName a &lt;code&gt;String&lt;/code&gt; object that must match the
                      SQL name of a column in this rowset, ignoring case                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <throws>
                    SQLException if (1) the given column name does not match the
                      name of a column in this rowset, (2) the cursor is not on
                      one of this rowset&apos;s rows or its insert row, or (3) this
                      rowset is &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateFloat" type="void" line="874">
                <params>
                    <param name="columnName" type="String"/>
                    <param name="x" type="float"/>
                </params>
            </method>
            <javadoc line="879">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;double&lt;/code&gt; value.
                  This method updates a column value in either the current row or
                  the insert row of this rowset, but it does not update the
                  database.  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnIndex the first column is &lt;code&gt;1&lt;/code&gt;, the second
                      is &lt;code&gt;2&lt;/code&gt;, and so on; must be &lt;code&gt;1&lt;/code&gt; or larger
                      and equal to or less than the number of columns in this rowset                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <throws>
                    SQLException if (1) the given column index is out of bounds,
                      (2) the cursor is not on one of this rowset&apos;s rows or its
                      insert row, or (3) this rowset is
                      &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateDouble" type="void" line="902">
                <params>
                    <param name="columnIndex" type="int"/>
                    <param name="x" type="double"/>
                </params>
                <declaration name="bool" type="boolean" line="904"/>
                <scope line="906">
                    <scope line="907">
                        <scope line="910"/>
                    </scope>
                </scope>
            </method>
            <javadoc line="919">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;double&lt;/code&gt; value.
                  This method updates a column value in either the current row or
                  the insert row of this rowset, but it does not update the
                  database.  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnName a &lt;code&gt;String&lt;/code&gt; object that must match the
                      SQL name of a column in this rowset, ignoring case                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <throws>
                    SQLException if (1) the given column name does not match the
                      name of a column in this rowset, (2) the cursor is not on
                      one of this rowset&apos;s rows or its insert row, or (3) this
                      rowset is &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateDouble" type="void" line="941">
                <params>
                    <param name="columnName" type="String"/>
                    <param name="x" type="double"/>
                </params>
            </method>
            <javadoc line="946">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;java.math.BigDecimal&lt;/code&gt; object.
                  &lt;P&gt;
                  This method updates a column value in the current row or the insert
                  row of this rowset, but it does not update the database.
                  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnIndex the first column is &lt;code&gt;1&lt;/code&gt;, the second
                      is &lt;code&gt;2&lt;/code&gt;, and so on; must be &lt;code&gt;1&lt;/code&gt; or larger
                      and equal to or less than the number of columns in this rowset                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <throws>
                    SQLException if (1) the given column index is out of bounds,
                      (2) the cursor is not on one of this rowset&apos;s rows or its
                      insert row, or (3) this rowset is
                      &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateBigDecimal" type="void" line="969">
                <params>
                    <param name="columnIndex" type="int"/>
                    <param name="x" type="BigDecimal"/>
                </params>
                <declaration name="bool" type="boolean" line="971"/>
                <scope line="973">
                    <scope line="974">
                        <scope line="977"/>
                    </scope>
                </scope>
            </method>
            <javadoc line="986">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;java.math.BigDecimal&lt;/code&gt; object.
                  &lt;P&gt;
                  This method updates a column value in the current row or the insert
                  row of this rowset, but it does not update the database.
                  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnName a &lt;code&gt;String&lt;/code&gt; object that must match the
                      SQL name of a column in this rowset, ignoring case                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <throws>
                    SQLException if (1) the given column name does not match the
                      name of a column in this rowset, (2) the cursor is not on
                      one of this rowset&apos;s rows or its insert row, or (3) this
                      rowset is &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateBigDecimal" type="void" line="1008">
                <params>
                    <param name="columnName" type="String"/>
                    <param name="x" type="BigDecimal"/>
                </params>
            </method>
            <javadoc line="1013">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;String&lt;/code&gt; object.
                  &lt;P&gt;
                  This method updates a column value in either the current row or
                  the insert row of this rowset, but it does not update the
                  database.  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to mark the row as updated.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called to insert the new row into this rowset and mark it
                  as inserted. Both of these methods must be called before the
                  cursor moves to another row.
                  &lt;P&gt;
                  The method &lt;code&gt;acceptChanges&lt;/code&gt; must be called if the
                  updated values are to be written back to the underlying database.                
                <param>
                    columnIndex the first column is &lt;code&gt;1&lt;/code&gt;, the second
                      is &lt;code&gt;2&lt;/code&gt;, and so on; must be &lt;code&gt;1&lt;/code&gt; or larger
                      and equal to or less than the number of columns in this rowset                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <throws>
                    SQLException if (1) the given column index is out of bounds,
                      (2) the cursor is not on one of this rowset&apos;s rows or its
                      insert row, or (3) this rowset is
                      &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateString" type="void" line="1039">
                <params>
                    <param name="columnIndex" type="int"/>
                    <param name="x" type="String"/>
                </params>
                <declaration name="bool" type="boolean" line="1041"/>
                <scope line="1043">
                    <scope line="1044">
                        <scope line="1047"/>
                    </scope>
                </scope>
            </method>
            <javadoc line="1056">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;String&lt;/code&gt; object.
                  This method updates a column value in either the current row or
                  the insert row of this rowset, but it does not update the
                  database.  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnName a &lt;code&gt;String&lt;/code&gt; object that must match the
                      SQL name of a column in this rowset, ignoring case                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <throws>
                    SQLException if (1) the given column name does not match the
                      name of a column in this rowset, (2) the cursor is not on
                      one of this rowset&apos;s rows or its insert row, or (3) this
                      rowset is &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateString" type="void" line="1078">
                <params>
                    <param name="columnName" type="String"/>
                    <param name="x" type="String"/>
                </params>
            </method>
            <javadoc line="1083">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;byte&lt;/code&gt; array.
                  This method updates a column value in either the current row or
                  the insert row of this rowset, but it does not update the
                  database.  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnIndex the first column is &lt;code&gt;1&lt;/code&gt;, the second
                      is &lt;code&gt;2&lt;/code&gt;, and so on; must be &lt;code&gt;1&lt;/code&gt; or larger
                      and equal to or less than the number of columns in this rowset                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <throws>
                    SQLException if (1) the given column index is out of bounds,
                      (2) the cursor is not on one of this rowset&apos;s rows or its
                      insert row, or (3) this rowset is
                      &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateBytes" type="void" line="1106">
                <params>
                    <param name="columnIndex" type="int"/>
                    <param name="x" type="byte[]"/>
                </params>
                <declaration name="bool" type="boolean" line="1108"/>
                <declaration name="val" type="String" line="1109"/>
                <declaration name="obj_arr" type="Byte[]" line="1111"/>
                <scope line="1113"/>
                <scope line="1119">
                    <scope line="1120">
                        <scope line="1123"/>
                    </scope>
                </scope>
            </method>
            <javadoc line="1132">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;byte&lt;/code&gt; array.
                  This method updates a column value in either the current row or
                  the insert row of this rowset, but it does not update the
                  database.  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnName a &lt;code&gt;String&lt;/code&gt; object that must match the
                      SQL name of a column in this rowset, ignoring case                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <throws>
                    SQLException if (1) the given column name does not match the
                      name of a column in this rowset, (2) the cursor is not on
                      one of this rowset&apos;s rows or its insert row, or (3) this
                      rowset is &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateBytes" type="void" line="1154">
                <params>
                    <param name="columnName" type="String"/>
                    <param name="x" type="byte[]"/>
                </params>
            </method>
            <javadoc line="1159">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;Date&lt;/code&gt; object.
                  This method updates a column value in either the current row or
                  the insert row of this rowset, but it does not update the
                  database.  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnIndex the first column is &lt;code&gt;1&lt;/code&gt;, the second
                      is &lt;code&gt;2&lt;/code&gt;, and so on; must be &lt;code&gt;1&lt;/code&gt; or larger
                      and equal to or less than the number of columns in this rowset                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <throws>
                    SQLException if (1) the given column index is out of bounds,
                      (2) the cursor is not on one of this rowset&apos;s rows or its
                      insert row, (3) the type of the designated column is not
                      an SQL &lt;code&gt;DATE&lt;/code&gt; or &lt;code&gt;TIMESTAMP&lt;/code&gt;, or
                      (4) this rowset is &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateDate" type="void" line="1183">
                <params>
                    <param name="columnIndex" type="int"/>
                    <param name="x" type="java.sql.Date"/>
                </params>
                <declaration name="bool" type="boolean" line="1185"/>
                <scope line="1187">
                    <scope line="1188">
                        <scope line="1191"/>
                    </scope>
                </scope>
            </method>
            <javadoc line="1200">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;Date&lt;/code&gt; object.
                  This method updates a column value in either the current row or
                  the insert row of this rowset, but it does not update the
                  database.  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnName a &lt;code&gt;String&lt;/code&gt; object that must match the
                      SQL name of a column in this rowset, ignoring case                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <throws>
                    SQLException if (1) the given column name does not match the
                      name of a column in this rowset, (2) the cursor is not on
                      one of this rowset&apos;s rows or its insert row, (3) the type
                      of the designated column is not an SQL &lt;code&gt;DATE&lt;/code&gt; or
                      &lt;code&gt;TIMESTAMP&lt;/code&gt;, or (4) this rowset is
                      &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateDate" type="void" line="1224">
                <params>
                    <param name="columnName" type="String"/>
                    <param name="x" type="java.sql.Date"/>
                </params>
            </method>
            <javadoc line="1229">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;Time&lt;/code&gt; object.
                  This method updates a column value in either the current row or
                  the insert row of this rowset, but it does not update the
                  database.  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnIndex the first column is &lt;code&gt;1&lt;/code&gt;, the second
                      is &lt;code&gt;2&lt;/code&gt;, and so on; must be &lt;code&gt;1&lt;/code&gt; or larger
                      and equal to or less than the number of columns in this rowset                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <throws>
                    SQLException if (1) the given column index is out of bounds,
                      (2) the cursor is not on one of this rowset&apos;s rows or its
                      insert row, (3) the type of the designated column is not
                      an SQL &lt;code&gt;TIME&lt;/code&gt; or &lt;code&gt;TIMESTAMP&lt;/code&gt;, or
                      (4) this rowset is &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateTime" type="void" line="1253">
                <params>
                    <param name="columnIndex" type="int"/>
                    <param name="x" type="Time"/>
                </params>
                <declaration name="bool" type="boolean" line="1255"/>
                <scope line="1257">
                    <scope line="1258">
                        <scope line="1261"/>
                    </scope>
                </scope>
            </method>
            <javadoc line="1270">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;Time&lt;/code&gt; object.
                  This method updates a column value in either the current row or
                  the insert row of this rowset, but it does not update the
                  database.  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnName a &lt;code&gt;String&lt;/code&gt; object that must match the
                      SQL name of a column in this rowset, ignoring case                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <throws>
                    SQLException if (1) the given column name does not match the
                      name of a column in this rowset, (2) the cursor is not on
                      one of this rowset&apos;s rows or its insert row, (3) the type
                      of the designated column is not an SQL &lt;code&gt;TIME&lt;/code&gt; or
                      &lt;code&gt;TIMESTAMP&lt;/code&gt;, or (4) this rowset is
                      &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateTime" type="void" line="1294">
                <params>
                    <param name="columnName" type="String"/>
                    <param name="x" type="Time"/>
                </params>
            </method>
            <javadoc line="1299">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;Timestamp&lt;/code&gt; object.
                  This method updates a column value in either the current row or
                  the insert row of this rowset, but it does not update the
                  database.  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnIndex the first column is &lt;code&gt;1&lt;/code&gt;, the second
                      is &lt;code&gt;2&lt;/code&gt;, and so on; must be &lt;code&gt;1&lt;/code&gt; or larger
                      and equal to or less than the number of columns in this rowset                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <throws>
                    SQLException if (1) the given column index is out of bounds,
                      (2) the cursor is not on one of this rowset&apos;s rows or its
                      insert row, (3) the type of the designated column is not
                      an SQL &lt;code&gt;DATE&lt;/code&gt;, &lt;code&gt;TIME&lt;/code&gt;, or
                      &lt;code&gt;TIMESTAMP&lt;/code&gt;, or (4) this rowset is
                      &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateTimestamp" type="void" line="1324">
                <params>
                    <param name="columnIndex" type="int"/>
                    <param name="x" type="Timestamp"/>
                </params>
                <declaration name="bool" type="boolean" line="1326"/>
                <scope line="1328">
                    <scope line="1329">
                        <scope line="1332"/>
                    </scope>
                </scope>
            </method>
            <javadoc line="1341">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;Timestamp&lt;/code&gt; object.
                  This method updates a column value in either the current row or
                  the insert row of this rowset, but it does not update the
                  database.  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnName a &lt;code&gt;String&lt;/code&gt; object that must match the
                      SQL name of a column in this rowset, ignoring case                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <throws>
                    SQLException if the given column index is out of bounds or
                      the cursor is not on one of this rowset&apos;s rows or its
                      insert row                    
                </throws>
                <throws>
                    SQLException if (1) the given column name does not match the
                      name of a column in this rowset, (2) the cursor is not on
                      one of this rowset&apos;s rows or its insert row, (3) the type
                      of the designated column is not an SQL &lt;code&gt;DATE&lt;/code&gt;,
                      &lt;code&gt;TIME&lt;/code&gt;, or &lt;code&gt;TIMESTAMP&lt;/code&gt;, or (4) this
                      rowset is &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateTimestamp" type="void" line="1368">
                <params>
                    <param name="columnName" type="String"/>
                    <param name="x" type="Timestamp"/>
                </params>
            </method>
            <javadoc line="1373">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  ASCII stream value.
                  &lt;P&gt;
                  This method updates a column value in either the current row or
                  the insert row of this rowset, but it does not update the
                  database.  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnIndex the first column is &lt;code&gt;1&lt;/code&gt;, the second
                      is &lt;code&gt;2&lt;/code&gt;, and so on; must be &lt;code&gt;1&lt;/code&gt; or larger
                      and equal to or less than the number of columns in this rowset                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <param>
                    length the number of one-byte ASCII characters in the stream                    
                </param>
                <throws>
                    SQLException if this method is invoked                    
                </throws>
            </javadoc>
            <method name="updateAsciiStream" type="void" line="1394">
                <params>
                    <param name="columnIndex" type="int"/>
                    <param name="x" type="java.io.InputStream"/>
                    <param name="length" type="int"/>
                </params>
                <declaration name="bool" type="boolean" line="1396"/>
                <scope line="1398">
                    <scope line="1399">
                        <scope line="1402"/>
                    </scope>
                </scope>
            </method>
            <javadoc line="1411">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  ASCII stream value.
                  &lt;P&gt;
                  This method updates a column value in either the current row or
                  the insert row of this rowset, but it does not update the
                  database.  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnName a &lt;code&gt;String&lt;/code&gt; object that must match the
                      SQL name of a column in this rowset, ignoring case                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <param>
                    length the number of one-byte ASCII characters in the stream                    
                </param>
            </javadoc>
            <method name="updateAsciiStream" type="void" line="1430">
                <params>
                    <param name="columnName" type="String"/>
                    <param name="x" type="java.io.InputStream"/>
                    <param name="length" type="int"/>
                </params>
            </method>
            <javadoc line="1435">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;java.io.Reader&lt;/code&gt; object.
                  &lt;P&gt;
                  This method updates a column value in either the current row or
                  the insert row of this rowset, but it does not update the
                  database.  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnIndex the first column is &lt;code&gt;1&lt;/code&gt;, the second
                      is &lt;code&gt;2&lt;/code&gt;, and so on; must be &lt;code&gt;1&lt;/code&gt; or larger
                      and equal to or less than the number of columns in this rowset                    
                </param>
                <param>
                    x the new column value; must be a &lt;code&gt;java.io.Reader&lt;/code&gt;
                      containing &lt;code&gt;BINARY&lt;/code&gt;, &lt;code&gt;VARBINARY&lt;/code&gt;,
                      &lt;code&gt;LONGVARBINARY&lt;/code&gt;, &lt;code&gt;CHAR&lt;/code&gt;, &lt;code&gt;VARCHAR&lt;/code&gt;,
                      or &lt;code&gt;LONGVARCHAR&lt;/code&gt; data                    
                </param>
                <param>
                    length the length of the stream in characters                    
                </param>
                <throws>
                    SQLException if (1) the given column index is out of bounds,
                      (2) the cursor is not on one of this rowset&apos;s rows or its
                      insert row, (3) the data in the stream is not a binary or
                      character type, or (4) this rowset is
                      &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateCharacterStream" type="void" line="1463">
                <params>
                    <param name="columnIndex" type="int"/>
                    <param name="x" type="java.io.Reader"/>
                    <param name="length" type="int"/>
                </params>
                <declaration name="bool" type="boolean" line="1465"/>
                <scope line="1467">
                    <scope line="1468">
                        <scope line="1471"/>
                    </scope>
                </scope>
            </method>
            <javadoc line="1480">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;java.io.Reader&lt;/code&gt; object.
                  &lt;P&gt;
                  This method updates a column value in either the current row or
                  the insert row of this rowset, but it does not update the
                  database.  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnName a &lt;code&gt;String&lt;/code&gt; object that must match the
                      SQL name of a column in this rowset, ignoring case                    
                </param>
                <param>
                    reader the new column value; must be a
                      &lt;code&gt;java.io.Reader&lt;/code&gt; containing &lt;code&gt;BINARY&lt;/code&gt;,
                      &lt;code&gt;VARBINARY&lt;/code&gt;, &lt;code&gt;LONGVARBINARY&lt;/code&gt;, &lt;code&gt;CHAR&lt;/code&gt;,
                      &lt;code&gt;VARCHAR&lt;/code&gt;, or &lt;code&gt;LONGVARCHAR&lt;/code&gt; data                    
                </param>
                <param>
                    length the length of the stream in characters                    
                </param>
                <throws>
                    SQLException if (1) the given column name does not match the
                      name of a column in this rowset, (2) the cursor is not on
                      one of this rowset&apos;s rows or its insert row, (3) the data
                      in the stream is not a binary or character type, or (4) this
                      rowset is &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateCharacterStream" type="void" line="1507">
                <params>
                    <param name="columnName" type="String"/>
                    <param name="reader" type="java.io.Reader"/>
                    <param name="length" type="int"/>
                </params>
            </method>
            <javadoc line="1511">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;java.io.InputStream&lt;/code&gt; object.
                  &lt;P&gt;
                  This method updates a column value in either the current row or
                  the insert row of this rowset, but it does not update the
                  database.  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnIndex the first column is &lt;code&gt;1&lt;/code&gt;, the second
                      is &lt;code&gt;2&lt;/code&gt;, and so on; must be &lt;code&gt;1&lt;/code&gt; or larger
                      and equal to or less than the number of columns in this rowset                    
                </param>
                <param>
                    x the new column value; must be a &lt;code&gt;java.io.InputStream&lt;/code&gt;
                      containing &lt;code&gt;BINARY&lt;/code&gt;, &lt;code&gt;VARBINARY&lt;/code&gt;, or
                      &lt;code&gt;LONGVARBINARY&lt;/code&gt; data                    
                </param>
                <param>
                    length the length of the stream in bytes                    
                </param>
                <throws>
                    SQLException if (1) the given column index is out of bounds,
                      (2) the cursor is not on one of this rowset&apos;s rows or its
                      insert row, (3) the data in the stream is not binary, or
                      (4) this rowset is &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateBinaryStream" type="void" line="1537">
                <params>
                    <param name="columnIndex" type="int"/>
                    <param name="x" type="java.io.InputStream"/>
                    <param name="length" type="int"/>
                </params>
                <declaration name="bool" type="boolean" line="1539"/>
                <scope line="1541">
                    <scope line="1542">
                        <scope line="1545"/>
                    </scope>
                </scope>
            </method>
            <javadoc line="1554">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;java.io.InputStream&lt;/code&gt; object.
                  &lt;P&gt;
                  This method updates a column value in either the current row or
                  the insert row of this rowset, but it does not update the
                  database.  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnName a &lt;code&gt;String&lt;/code&gt; object that must match the
                      SQL name of a column in this rowset, ignoring case                    
                </param>
                <param>
                    x the new column value; must be a &lt;code&gt;java.io.InputStream&lt;/code&gt;
                      containing &lt;code&gt;BINARY&lt;/code&gt;, &lt;code&gt;VARBINARY&lt;/code&gt;, or
                      &lt;code&gt;LONGVARBINARY&lt;/code&gt; data                    
                </param>
                <param>
                    length the length of the stream in bytes                    
                </param>
                <throws>
                    SQLException if (1) the given column name does not match the
                      name of a column in this rowset, (2) the cursor is not on
                      one of this rowset&apos;s rows or its insert row, (3) the data
                      in the stream is not binary, or (4) this rowset is
                      &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateBinaryStream" type="void" line="1580">
                <params>
                    <param name="columnName" type="String"/>
                    <param name="x" type="java.io.InputStream"/>
                    <param name="length" type="int"/>
                </params>
            </method>
            <javadoc line="1585">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;Object&lt;/code&gt; value.
                  &lt;P&gt;
                  This method updates a column value in either the current row or
                  the insert row of this rowset, but it does not update the
                  database.  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnIndex the first column is &lt;code&gt;1&lt;/code&gt;, the second
                      is &lt;code&gt;2&lt;/code&gt;, and so on; must be &lt;code&gt;1&lt;/code&gt; or larger
                      and equal to or less than the number of columns in this rowset                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <throws>
                    SQLException if (1) the given column index is out of bounds,
                      (2) the cursor is not on one of this rowset&apos;s rows or its
                      insert row, or (3) this rowset is
                      &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateObject" type="void" line="1608">
                <params>
                    <param name="columnIndex" type="int"/>
                    <param name="x" type="Object"/>
                </params>
                <declaration name="bool" type="boolean" line="1610"/>
                <scope line="1612">
                    <scope line="1613">
                        <scope line="1616"/>
                    </scope>
                </scope>
            </method>
            <javadoc line="1625">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;Object&lt;/code&gt; value.
                  &lt;P&gt;
                  This method updates a column value in either the current row or
                  the insert row of this rowset, but it does not update the
                  database.  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnName a &lt;code&gt;String&lt;/code&gt; object that must match the
                      SQL name of a column in this rowset, ignoring case                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <throws>
                    SQLException if (1) the given column name does not match the
                      name of a column in this rowset, (2) the cursor is not on
                      one of this rowset&apos;s rows or its insert row, or (3) this
                      rowset is &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateObject" type="void" line="1647">
                <params>
                    <param name="columnName" type="String"/>
                    <param name="x" type="Object"/>
                </params>
            </method>
            <javadoc line="1652">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;Object&lt;/code&gt; value.  The &lt;code&gt;scale&lt;/code&gt; parameter indicates
                  the number of digits to the right of the decimal point and is ignored
                  if the new column value is not a type that will be mapped to an SQL
                  &lt;code&gt;DECIMAL&lt;/code&gt; or &lt;code&gt;NUMERIC&lt;/code&gt; value.
                  &lt;P&gt;
                  This method updates a column value in either the current row or
                  the insert row of this rowset, but it does not update the
                  database.  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnIndex the first column is &lt;code&gt;1&lt;/code&gt;, the second
                      is &lt;code&gt;2&lt;/code&gt;, and so on; must be &lt;code&gt;1&lt;/code&gt; or larger
                      and equal to or less than the number of columns in this rowset                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <param>
                    scale the number of digits to the right of the decimal point (for
                      &lt;code&gt;DECIMAL&lt;/code&gt; and &lt;code&gt;NUMERIC&lt;/code&gt; types only)                    
                </param>
                <throws>
                    SQLException if (1) the given column index is out of bounds,
                      (2) the cursor is not on one of this rowset&apos;s rows or its
                      insert row, or (3) this rowset is
                      &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateObject" type="void" line="1680">
                <params>
                    <param name="columnIndex" type="int"/>
                    <param name="x" type="Object"/>
                    <param name="scale" type="int"/>
                </params>
                <declaration name="bool" type="boolean" line="1682"/>
                <scope line="1684">
                    <scope line="1685">
                        <scope line="1688"/>
                    </scope>
                </scope>
            </method>
            <javadoc line="1697">
                Sets the designated column in either the current row or the insert
                  row of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object with the given
                  &lt;code&gt;Object&lt;/code&gt; value.  The &lt;code&gt;scale&lt;/code&gt; parameter
                  indicates the number of digits to the right of the decimal point
                  and is ignored if the new column value is not a type that will be
                  mapped to an SQL &lt;code&gt;DECIMAL&lt;/code&gt; or &lt;code&gt;NUMERIC&lt;/code&gt; value.
                  &lt;P&gt;
                  This method updates a column value in either the current row or
                  the insert row of this rowset, but it does not update the
                  database.  If the cursor is on a row in the rowset, the
                  method {@link #updateRow} must be called to update the database.
                  If the cursor is on the insert row, the method {@link #insertRow}must be called, which will insert the new row into both this rowset
                  and the database. Both of these methods must be called before the
                  cursor moves to another row.                
                <param>
                    columnName a &lt;code&gt;String&lt;/code&gt; object that must match the
                      SQL name of a column in this rowset, ignoring case                    
                </param>
                <param>
                    x the new column value                    
                </param>
                <param>
                    scale the number of digits to the right of the decimal point (for
                      &lt;code&gt;DECIMAL&lt;/code&gt; and &lt;code&gt;NUMERIC&lt;/code&gt; types only)                    
                </param>
                <throws>
                    SQLException if (1) the given column name does not match the
                      name of a column in this rowset, (2) the cursor is not on
                      one of this rowset&apos;s rows or its insert row, or (3) this
                      rowset is &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="updateObject" type="void" line="1724">
                <params>
                    <param name="columnName" type="String"/>
                    <param name="x" type="Object"/>
                    <param name="scale" type="int"/>
                </params>
            </method>
            <javadoc line="1729">
                Inserts the contents of this &lt;code&gt;CachedRowSetImpl&lt;/code&gt; object&apos;s insert
                  row into this rowset immediately following the current row.
                  If the current row is the
                  position after the last row or before the first row, the new row will
                  be inserted at the end of the rowset.  This method also notifies
                  listeners registered with this rowset that the row has changed.
                  &lt;P&gt;
                  The cursor must be on the insert row when this method is called.                
                <throws>
                    SQLException if (1) the cursor is not on the insert row,
                      (2) one or more of the non-nullable columns in the insert
                      row has not been given a value, or (3) this rowset is
                      &lt;code&gt;ResultSet.CONCUR_READ_ONLY&lt;/code&gt;                    
                </throws>
            </javadoc>
            <method name="insertRow" type="void" line="1744"/>
            <javadoc line="1750">
                This method re populates the resBundle
                  during the deserialization process                
            </javadoc>
            <method name="readObject" type="void" line="1755">
                <params>
                    <param name="ois" type="ObjectInputStream"/>
                </params>
                <comment line="1757">
                    Default state initialization happens here                    
                </comment>
                <comment line="1759">
                    Initialization of transient Res Bundle happens here .                    
                </comment>
                <scope line="1759"/>
                <scope line="1761"/>
            </method>
            <declaration name="serialVersionUID" type="long" line="1767"/>
        </class>
    </source>